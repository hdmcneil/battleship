require 'spec_helper'

RSpec.describe Game do
  before :each do
    @game = Game.new
    @computer_player = ComputerPlayer.new
    @player = Player.new
  end

  it 'initializes the game state correctly' do
    
  end
end